import "./App.scss"

export const App = () => {
    return (
        <form>
            <input type="text" placeholder="Почта" />
            <input type="text" placeholder="Пароль" />
            <button>Готово</button>
        </form>

    )
}